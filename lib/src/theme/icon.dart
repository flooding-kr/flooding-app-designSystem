import 'package:flutter/material.dart';
import 'package:flutter_svg/svg.dart';
import '../../../flooding_app_design_system.dart';

class FloodingIcon {
  static const String _iconPath =
      'packages/flooding_app_design_system/assets/image/';

  static Widget account({
    Color color = FloodingColor.black,
    double width = 24,
    double height = 24,
  }) {
    return SvgPicture.asset(
      '${_iconPath}account.svg',
      colorFilter: ColorFilter.mode(color, BlendMode.srcIn),
      width: width,
      height: height,
    );
  }

  static Widget add({
    Color color = FloodingColor.black,
    double width = 24,
    double height = 24,
  }) {
    return SvgPicture.asset(
      '${_iconPath}add.svg',
      colorFilter: ColorFilter.mode(color, BlendMode.srcIn),
      width: width,
      height: height,
    );
  }

  static Widget all({
    Color color = FloodingColor.black,
    double width = 24,
    double height = 24,
  }) {
    return SvgPicture.asset(
      '${_iconPath}all.svg',
      colorFilter: ColorFilter.mode(color, BlendMode.srcIn),
      width: width,
      height: height,
    );
  }

  static Widget arrowDown({
    Color color = FloodingColor.black,
    double width = 24,
    double height = 24,
  }) {
    return SvgPicture.asset(
      '${_iconPath}arrow_down.svg',
      colorFilter: ColorFilter.mode(color, BlendMode.srcIn),
      width: width,
      height: height,
    );
  }

  static Widget arrowForward({
    Color color = FloodingColor.black,
    double width = 24,
    double height = 24,
  }) {
    return SvgPicture.asset(
      '${_iconPath}arrow_forward.svg',
      colorFilter: ColorFilter.mode(color, BlendMode.srcIn),
      width: width,
      height: height,
    );
  }

  static Widget arrowLeft({
    Color color = FloodingColor.black,
    double width = 24,
    double height = 24,
  }) {
    return SvgPicture.asset(
      '${_iconPath}arrow_left.svg',
      colorFilter: ColorFilter.mode(color, BlendMode.srcIn),
      width: width,
      height: height,
    );
  }

  static Widget arrowRight({
    Color color = FloodingColor.black,
    double width = 24,
    double height = 24,
  }) {
    return SvgPicture.asset(
      '${_iconPath}arrow_right.svg',
      colorFilter: ColorFilter.mode(color, BlendMode.srcIn),
      width: width,
      height: height,
    );
  }

  static Widget arrowUp({
    Color color = FloodingColor.black,
    double width = 24,
    double height = 24,
  }) {
    return SvgPicture.asset(
      '${_iconPath}arrow_up.svg',
      colorFilter: ColorFilter.mode(color, BlendMode.srcIn),
      width: width,
      height: height,
    );
  }

  static Widget attendance({
    Color color = FloodingColor.black,
    double width = 24,
    double height = 24,
  }) {
    return SvgPicture.asset(
      '${_iconPath}attendance.svg',
      colorFilter: ColorFilter.mode(color, BlendMode.srcIn),
      width: width,
      height: height,
    );
  }

  static Widget bellDisable({
    Color color = FloodingColor.black,
    double width = 24,
    double height = 24,
  }) {
    return SvgPicture.asset(
      '${_iconPath}bell_disable.svg',
      colorFilter: ColorFilter.mode(color, BlendMode.srcIn),
      width: width,
      height: height,
    );
  }

  static Widget bellDisableOutline({
    Color color = FloodingColor.black,
    double width = 24,
    double height = 24,
  }) {
    return SvgPicture.asset(
      '${_iconPath}bell_disable_outline.svg',
      colorFilter: ColorFilter.mode(color, BlendMode.srcIn),
      width: width,
      height: height,
    );
  }

  static Widget bellEnable({
    Color color = FloodingColor.black,
    double width = 24,
    double height = 24,
  }) {
    return SvgPicture.asset(
      '${_iconPath}bell_enable.svg',
      colorFilter: ColorFilter.mode(color, BlendMode.srcIn),
      width: width,
      height: height,
    );
  }

  static Widget bellEnableOutline({
    Color color = FloodingColor.black,
    double width = 24,
    double height = 24,
  }) {
    return SvgPicture.asset(
      '${_iconPath}bell_enable_outline.svg',
      colorFilter: ColorFilter.mode(color, BlendMode.srcIn),
      width: width,
      height: height,
    );
  }

  static Widget check({
    Color color = FloodingColor.black,
    double width = 24,
    double height = 24,
  }) {
    return SvgPicture.asset(
      '${_iconPath}check.svg',
      colorFilter: ColorFilter.mode(color, BlendMode.srcIn),
      width: width,
      height: height,
    );
  }

  static Widget checkBox({
    Color color = FloodingColor.black,
    double width = 24,
    double height = 24,
  }) {
    return SvgPicture.asset(
      '${_iconPath}check_box.svg',
      colorFilter: ColorFilter.mode(color, BlendMode.srcIn),
      width: width,
      height: height,
    );
  }

  static Widget checkBoxOutline({
    Color color = FloodingColor.black,
    double width = 24,
    double height = 24,
  }) {
    return SvgPicture.asset(
      '${_iconPath}check_box_outline.svg',
      colorFilter: ColorFilter.mode(color, BlendMode.srcIn),
      width: width,
      height: height,
    );
  }

  static Widget chevronDown({
    Color color = FloodingColor.black,
    double width = 24,
    double height = 24,
  }) {
    return SvgPicture.asset(
      '${_iconPath}chevron_down.svg',
      colorFilter: ColorFilter.mode(color, BlendMode.srcIn),
      width: width,
      height: height,
    );
  }

  static Widget chair({
    Color color = FloodingColor.black,
    double width = 24,
    double height = 24,
  }) {
    return SvgPicture.asset(
      '${_iconPath}chier.svg',
      colorFilter: ColorFilter.mode(color, BlendMode.srcIn),
      width: width,
      height: height,
    );
  }

  static Widget closeBig({
    Color color = FloodingColor.black,
    double width = 24,
    double height = 24,
  }) {
    return SvgPicture.asset(
      '${_iconPath}close_big.svg',
      colorFilter: ColorFilter.mode(color, BlendMode.srcIn),
      width: width,
      height: height,
    );
  }

  static Widget closeSmall({
    Color color = FloodingColor.black,
    double width = 24,
    double height = 24,
  }) {
    return SvgPicture.asset(
      '${_iconPath}close_small.svg',
      colorFilter: ColorFilter.mode(color, BlendMode.srcIn),
      width: width,
      height: height,
    );
  }

  static Widget club({
    Color color = FloodingColor.black,
    double width = 24,
    double height = 24,
  }) {
    return SvgPicture.asset(
      '${_iconPath}club.svg',
      colorFilter: ColorFilter.mode(color, BlendMode.srcIn),
      width: width,
      height: height,
    );
  }

  static Widget dormitory({
    Color color = FloodingColor.black,
    double width = 24,
    double height = 24,
  }) {
    return SvgPicture.asset(
      '${_iconPath}dormitory.svg',
      colorFilter: ColorFilter.mode(color, BlendMode.srcIn),
      width: width,
      height: height,
    );
  }

  static Widget error({
    Color color = FloodingColor.black,
    double width = 24,
    double height = 24,
  }) {
    return SvgPicture.asset(
      '${_iconPath}error.svg',
      colorFilter: ColorFilter.mode(color, BlendMode.srcIn),
      width: width,
      height: height,
    );
  }

  static Widget eyeClose({
    Color color = FloodingColor.black,
    double width = 24,
    double height = 24,
  }) {
    return SvgPicture.asset(
      '${_iconPath}eye_close.svg',
      colorFilter: ColorFilter.mode(color, BlendMode.srcIn),
      width: width,
      height: height,
    );
  }

  static Widget eyeOpen({
    Color color = FloodingColor.black,
    double width = 24,
    double height = 24,
  }) {
    return SvgPicture.asset(
      '${_iconPath}eye_open.svg',
      colorFilter: ColorFilter.mode(color, BlendMode.srcIn),
      width: width,
      height: height,
    );
  }

  static Widget filter({
    Color color = FloodingColor.black,
    double width = 24,
    double height = 24,
  }) {
    return SvgPicture.asset(
      '${_iconPath}filter.svg',
      colorFilter: ColorFilter.mode(color, BlendMode.srcIn),
      width: width,
      height: height,
    );
  }

  static Widget filterOutline({
    Color color = FloodingColor.black,
    double width = 24,
    double height = 24,
  }) {
    return SvgPicture.asset(
      '${_iconPath}filter_outline.svg',
      colorFilter: ColorFilter.mode(color, BlendMode.srcIn),
      width: width,
      height: height,
    );
  }

  static Widget genres({
    Color color = FloodingColor.black,
    double width = 24,
    double height = 24,
  }) {
    return SvgPicture.asset(
      '${_iconPath}genres.svg',
      colorFilter: ColorFilter.mode(color, BlendMode.srcIn),
      width: width,
      height: height,
    );
  }

  static Widget genresOutline({
    Color color = FloodingColor.black,
    double width = 24,
    double height = 24,
  }) {
    return SvgPicture.asset(
      '${_iconPath}genres_outline.svg',
      colorFilter: ColorFilter.mode(color, BlendMode.srcIn),
      width: width,
      height: height,
    );
  }

  static Widget help({
    Color color = FloodingColor.black,
    double width = 24,
    double height = 24,
  }) {
    return SvgPicture.asset(
      '${_iconPath}help.svg',
      colorFilter: ColorFilter.mode(color, BlendMode.srcIn),
      width: width,
      height: height,
    );
  }

  static Widget home({
    Color color = FloodingColor.black,
    double width = 24,
    double height = 24,
  }) {
    return SvgPicture.asset(
      '${_iconPath}home.svg',
      colorFilter: ColorFilter.mode(color, BlendMode.srcIn),
      width: width,
      height: height,
    );
  }

  static Widget homeOutline({
    Color color = FloodingColor.black,
    double width = 24,
    double height = 24,
  }) {
    return SvgPicture.asset(
      '${_iconPath}home_outline.svg',
      colorFilter: ColorFilter.mode(color, BlendMode.srcIn),
      width: width,
      height: height,
    );
  }

  static Widget homebase({
    Color color = FloodingColor.black,
    double width = 24,
    double height = 24,
  }) {
    return SvgPicture.asset(
      '${_iconPath}homebase.svg',
      colorFilter: ColorFilter.mode(color, BlendMode.srcIn),
      width: width,
      height: height,
    );
  }

  static Widget logout({
    Color color = FloodingColor.black,
    double width = 24,
    double height = 24,
  }) {
    return SvgPicture.asset(
      '${_iconPath}logout.svg',
      colorFilter: ColorFilter.mode(color, BlendMode.srcIn),
      width: width,
      height: height,
    );
  }

  static Widget manager({
    Color color = FloodingColor.black,
    double width = 24,
    double height = 24,
  }) {
    return SvgPicture.asset(
      '${_iconPath}manager.svg',
      colorFilter: ColorFilter.mode(color, BlendMode.srcIn),
      width: width,
      height: height,
    );
  }

  static Widget more({
    Color color = FloodingColor.black,
    double width = 24,
    double height = 24,
  }) {
    return SvgPicture.asset(
      '${_iconPath}more.svg',
      colorFilter: ColorFilter.mode(color, BlendMode.srcIn),
      width: width,
      height: height,
    );
  }

  static Widget recommend({
    Color color = FloodingColor.black,
    double width = 24,
    double height = 24,
  }) {
    return SvgPicture.asset(
      '${_iconPath}recommend.svg',
      colorFilter: ColorFilter.mode(color, BlendMode.srcIn),
      width: width,
      height: height,
    );
  }

  static Widget recommendOutline({
    Color color = FloodingColor.black,
    double width = 24,
    double height = 24,
  }) {
    return SvgPicture.asset(
      '${_iconPath}recommend_outline.svg',
      colorFilter: ColorFilter.mode(color, BlendMode.srcIn),
      width: width,
      height: height,
    );
  }

  static Widget schedule({
    Color color = FloodingColor.black,
    double width = 24,
    double height = 24,
  }) {
    return SvgPicture.asset(
      '${_iconPath}schedule.svg',
      colorFilter: ColorFilter.mode(color, BlendMode.srcIn),
      width: width,
      height: height,
    );
  }

  static Widget search({
    Color color = FloodingColor.black,
    double width = 24,
    double height = 24,
  }) {
    return SvgPicture.asset(
      '${_iconPath}search.svg',
      colorFilter: ColorFilter.mode(color, BlendMode.srcIn),
      width: width,
      height: height,
    );
  }

  static Widget send({
    Color color = FloodingColor.black,
    double width = 24,
    double height = 24,
  }) {
    return SvgPicture.asset(
      '${_iconPath}send.svg',
      colorFilter: ColorFilter.mode(color, BlendMode.srcIn),
      width: width,
      height: height,
    );
  }

  static Widget study({
    Color color = FloodingColor.black,
    double width = 24,
    double height = 24,
  }) {
    return SvgPicture.asset(
      '${_iconPath}study.svg',
      colorFilter: ColorFilter.mode(color, BlendMode.srcIn),
      width: width,
      height: height,
    );
  }

  static Widget time({
    Color color = FloodingColor.black,
    double width = 24,
    double height = 24,
  }) {
    return SvgPicture.asset(
      '${_iconPath}time.svg',
      colorFilter: ColorFilter.mode(color, BlendMode.srcIn),
      width: width,
      height: height,
    );
  }

  static Widget trash({
    Color color = FloodingColor.black,
    double width = 24,
    double height = 24,
  }) {
    return SvgPicture.asset(
      '${_iconPath}trash.svg',
      colorFilter: ColorFilter.mode(color, BlendMode.srcIn),
      width: width,
      height: height,
    );
  }

  static Widget write({
    Color color = FloodingColor.black,
    double width = 24,
    double height = 24,
  }) {
    return SvgPicture.asset(
      '${_iconPath}write.svg',
      colorFilter: ColorFilter.mode(color, BlendMode.srcIn),
      width: width,
      height: height,
    );
  }

  static Widget logo({
    Color color = FloodingColor.main600,
    double width = 226,
    double height = 86,
  }) {
    return SvgPicture.asset(
      '${_iconPath}logo.svg',
      colorFilter: ColorFilter.mode(color, BlendMode.srcIn),
      width: width,
      height: height,
    );
  }
}
